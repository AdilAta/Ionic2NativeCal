If Ionic is not setup:
Install ionic 2:
npm install -g ionic@beta
and Cordova:
npm install -g cordova



To run on browser:
run $ ionic serve

To run on android:

Add androi platform once:
ionic platform add android

Make android build:
run $ cordova build android
(locate the .apk file and install on android)